services:
  backend:
    build: ./backend
    ports:
      - "8080:8080"
    networks:
      - ycyw-net

  frontend:
    build: ./frontend
    ports:
      - "4200:80"
    depends_on:
      - backend
    networks:
      - ycyw-net

networks:
  ycyw-net:
    driver: bridge